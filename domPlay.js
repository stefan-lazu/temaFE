let moveButtons = document.querySelectorAll('#popup .arrowMove');

let ourMoveButton = moveButtons[0];

ourMoveeButton.addEventListener('click', function () {

    let ourPopup = document.getElementById('popup');
    ourPopup.style.display = 'block';
});